<form method="post" action="{{ path('app_comunidad_delete', {'id': comunidad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comunidad.id) }}">
    <button class="btn">Delete</button>
</form>
