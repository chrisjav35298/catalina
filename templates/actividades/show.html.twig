{% extends 'base.html.twig' %}

{% block title %}Actividad{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4 text-center">Detalle de la Actividad</h1>

        <div class="card">
            <div class="card-body">
                <table class="table table-bordered">
                    <tbody>
                        <tr>
                            <th scope="row" class="text-end">Id</th>
                            <td>{{ actividade.id }}</td>
                        </tr>
                        <tr>
                            <th scope="row" class="text-end">Descripción Corta</th>
                            <td>{{ actividade.descripcionCorta }}</td>
                        </tr>
                        <tr>
                            <th scope="row" class="text-end">Descripción Larga</th>
                            <td>{{ actividade.descripcionLarga }}</td>
                        </tr>
                        <tr>
                            <th scope="row" class="text-end">Imagen Destacada</th>
                            <td>
                                <img src="{{ asset('uploads/' ~ actividade.imagenDestacada) }}" alt="Imagen Destacada" class="img-thumbnail" style="width: 200px;">
                            </td>
                        </tr>
                        <tr>
                            <th scope="row" class="text-end">Imágenes</th>
                            <td>
                                {% for imagen in actividade.imagenes %}
                                    {% if imagen.ruta is not null %}
                                        <img src="{{ asset('uploads/' ~ imagen.ruta) }}" alt="Imagen" class="img-thumbnail me-2 mb-2" style="width: 100px;">
                                    {% endif %}
                                {% else %}
                                    <span class="text-muted">No hay imágenes</span>
                                {% endfor %}
                            </td>
                        </tr>
                    </tbody>
                </table>
                    <a href="{{ path('app_actividades_index') }}" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Volver a la lista
                    </a>
{# 
                <div class="d-flex justify-content-between mt-3">
                    <a href="{{ path('app_actividades_index') }}" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Volver a la lista
                    </a>
                    <a href="{{ path('app_actividades_edit', {'id': actividade.id}) }}" class="btn btn-primary">
                        <i class="bi bi-pencil"></i> Editar
                    </a>
                    {{ include('actividades/_delete_form.html.twig') }}
                </div> #}
            </div>
        </div>
    </div>
{% endblock %}
