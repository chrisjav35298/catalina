{% extends 'base.html.twig' %}

{% block title %}DocumentoInstitucional{% endblock %}

{% block body %}
    <h1>DocumentoInstitucional</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ documento_institucional.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ documento_institucional.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ documento_institucional.descripcion }}</td>
            </tr>
            <tr>
                <th>Archivo</th>
                <td>{{ documento_institucional.archivo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_documento_institucional_index') }}">back to list</a>

    <a href="{{ path('app_documento_institucional_edit', {'id': documento_institucional.id}) }}">edit</a>

    {{ include('documento_institucional/_delete_form.html.twig') }}
{% endblock %}
