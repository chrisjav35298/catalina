{% extends 'base.html.twig' %}

{% block title %}Equipo{% endblock %}

{% block body %}
<div class="container px-5 mt-4">
    <h1>Crear nuevo</h1>

    {{ include('equipo/_form.html.twig') }}

        <div class="d-flex justify-content-end align-items-center gap-2 m-5">
            <a href="{{ path('app_equipo_index') }}" class="btn btn-secondary">Volver a la lista</a>
        </div>
</div>

{% endblock %}











{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/equipo.js') }}"></script>
{% endblock %}
