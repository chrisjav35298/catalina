<form method="post" action="{{ path('app_carrusel_refugio_delete', {'id': carrusel_refugio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carrusel_refugio.id) }}">
    <button class="btn btn-danger ms-3">Delete</button>
</form>
