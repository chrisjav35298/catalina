

{{ form_start(form) }}
<div class="mb-4">
<h3 class="text-center mb-3">Detalles</h3>
    {{ form_row(form.titulo, {'attr': {'class': 'form-control','style': 'margin-bottom: 15px;'}}) }}
    {{ form_row(form.descripcionCorta, {'attr': {'class': 'form-control','style': 'margin-bottom: 15px;'}}) }}
    {{ form_row(form.descripcionDetallada, {'attr': {'class': 'form-control','style': 'margin-bottom: 15px;'}}) }}
    {{ form_row(form.imagen, {'attr': {'class': 'form-control','style': 'margin-bottom: 15px;'}}) }}
</div>
    <button class="btn btn-success">{{ button_label|default('Guardar') }}</button>
   </div>

{{ form_end(form) }}
